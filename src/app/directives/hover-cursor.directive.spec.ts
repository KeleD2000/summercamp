import { HoverCursorDirective } from './hover-cursor.directive';

describe('HoverCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverCursorDirective();
    expect(directive).toBeTruthy();
  });
});
